import { COLORS } from '~/theme/common'

const COLOR_PALETTE = {
  primary: {
    main: COLORS.palette.primaryColor,
  },
  secondary: {
    main: COLORS.palette.secondaryBlue1,
    dark: COLORS.gray.darkGray,
  },
  textPrimary: {
    main: COLORS.gray.textGray,
  },
  common: {
    black: '#000',
    white: COLORS.palette.white,
  },
}

export default COLOR_PALETTE
